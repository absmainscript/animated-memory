import React from "react";
import { Card, CardContent } from "@/components/ui/card";

export function HeroColorSettings({ configs }: { configs: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente HeroColorSettings em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}