import React from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

export function TestimonialsSectionTextsForm({ configs }: { configs: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente TestimonialsSectionTextsForm em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}