import React from "react";
import { Card, CardContent } from "@/components/ui/card";

export function SectionVisibilitySettings({ configs }: { configs: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente SectionVisibilitySettings em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}