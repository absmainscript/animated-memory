import React from "react";
import { Card, CardContent } from "@/components/ui/card";

export function SpecialtiesManager({ specialties }: { specialties: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente SpecialtiesManager em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}