import React from "react";
import { Card, CardContent } from "@/components/ui/card";

export function ContactScheduleManager({ contactSettings }: { contactSettings: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente ContactScheduleManager em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}