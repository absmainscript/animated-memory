import React from "react";
import { Card, CardContent } from "@/components/ui/card";

export function ServicesManager({ services }: { services: any[] }) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-sm text-gray-600">Componente ServicesManager em desenvolvimento</p>
      </CardContent>
    </Card>
  );
}